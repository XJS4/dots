# Dots
## List 
The file ```bash~/.xprofile``` configures background (using feh) and the compositor (using picom). My window manager is xmonad.

## Notes to self
### Commands
```bash
cat /sys/class/power_supply/BAT1/capacity # displays battery percentage
```

### st patches
| Name      | Version |
|--|--|
| Alpha     | (0.8.5) |
| Clipboard | (0.8.3) |
| Dynamic Cursor Colour | (0.9) |
| Ligatures | (0.9.2-alpha-scrollback) |
| Nord theme | (0.8.5) |
| Scrollback | (0.8.4) |

### Base XMonad keybinds
```
mod + p             = dmenu
mod + shift + enter = open terminal
mod + shift + c     = close window
```
